services:
  api:
    build: ./api
    restart: unless-stopped
    environment:
      - DATABASE_URL
      - SUPABASE_URL
      - SUPABASE_ANON_KEY
      - YOUTUBE_API_KEY
      - ODESLI_API_KEY
      - ALLOWED_ORIGINS
      - FRONTEND_URL
